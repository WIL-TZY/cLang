// Esse é um arquivo header
// Headers (arquivos .h) são usado em conjunto com arquivos C (.c) para incluir a declaração de variáveis, constantes ou funções
// Essas declarações criam protótipos dessas variáveis, constantes ou funções

// include guards
#ifndef MEU_MODULO_H
#define MEU_MODULO_H

// Explicações sobre as funções podem ser feitas aqui, ao invés do arquivo onde elas são implementadas

// Essa função requer que "n" seja um número não tão grande e também positivo
int soma (int n);

// Essa função requer que "n" seja um número não tão grande e também positivo
int fatorial (int n);

#endif